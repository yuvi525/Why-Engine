export const metadata = {
  title: "Analyze & Route | WHY Engine",
  description: "Test prompts through the intelligent routing pipeline and view live metrics."
};

export default function AnalyzeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
