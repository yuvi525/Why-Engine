export const metadata = {
  title: "Settings | WHY Engine",
  description: "Manage your API keys, organization configuration, and autopilot guardrails."
};

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
